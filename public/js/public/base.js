document.addEventListener('DOMContentLoaded', () => {
    const logo = document.getElementById('EpistemeSnow');

    logo.addEventListener('click', () => {
        createSnowflake();
    });

    
});
